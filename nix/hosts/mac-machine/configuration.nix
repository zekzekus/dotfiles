_: {
  # Host-specific nix-darwin overrides for mac-machine
  #
  # Common darwin system settings are in platforms/darwin/configuration.nix
  #
  # Examples:
  #   homebrew.casks = [ "some-host-specific-app" ];
  #   system.defaults.dock.autohide = false;
}
